# Dotii 1.0 开发指南

本文是 Dotii 固件和 Windows 管理中心的唯一开发说明，覆盖架构、环境、协议、模块扩展、构建、测试与发布。普通用户只需阅读 `README.md`。

## 1. 设计边界

Dotii 由两部分组成：ESP32-S3 固件负责设备交互，Windows 管理中心负责外部数据和配置。两者通过带令牌的局域网 HTTP 协议通信，蓝牙只用于发现、配网与恢复。

```text
Codex App Server ─┐
                  ├─ Dotii 管理中心 ── HTTP/schema v1 ── Dotii 固件
Bambu LAN MQTT ───┤          │
Bambu RTSPS ──────┘          └─ Windows BLE ── 配网/状态
```

面向用户的名称统一为“Dotii 管理中心”。`bridge`、`bridge_url`、`bridge_token` 只作为内部目录或协议字段使用。

## 2. 源码模块

- `main/main.c`：固件入口、页面注册与主循环。
- `main/app_state.*`：schema v1 快照解析和业务状态。
- `main/state_ui.*`：圆屏页面、动画、局部刷新和交互。
- `main/connectivity.*`：Wi-Fi、HTTP 快照和自定义帧同步。
- `main/device_config.*`：NVS 配置、显示和睡眠参数。
- `main/ble_bridge.*`：蓝牙 GATT 配置协议。
- `main/board_input.*`：触摸与物理按键。
- `main/generated/`：字体和图标生成结果，不手工编辑数据区。
- `components/waveshare__esp32_s3_touch_amoled_1_75/`：基于 Waveshare 3.0.1 的本地 BSP。项目修正了 CO5300 方向、原生 gap、软件合成旋转和 RGB565 脏矩形偶数对齐；更新上游 BSP 时必须逐项重新核对。
- `bridge/codex_bridge.py`：本机 Web/API 服务、配置存储和模块编排。
- `bridge/bridge_app.py`：Windows 托盘宿主、单实例和子进程生命周期。
- `bridge/codex_app_server.py`：Codex App Server 采集与容错。
- `bridge/bambu_client.py`：Bambu MQTT/TLS、相机与控制。
- `bridge/bluetooth_bridge.py`：Windows BLE 配网。
- `bridge/firmware_flasher.py`：设备识别、固件校验和 esptool 调用。
- `firmware/`：VBS 源码入口与 EXE 发布入口共用的最小烧录包。
- `bridge/runtime_paths.py`：源码与 PyInstaller 环境的统一资源定位。
- `bridge/web/`：本机管理网页，与源码和发布包共用。
- `bridge/tests/`：Python 单元和回归测试。
- `packaging/`：PyInstaller 与 Inno Setup 配置。

## 3. 环境准备

### 3.1 固件

安装 ESP-IDF 6.0.2。使用官方环境 PowerShell进入项目目录：

```powershell
idf.py --version
idf.py build
```

项目固定目标为 `esp32s3`，版本由根目录 `CMakeLists.txt` 的 `PROJECT_VER` 定义。`dependencies.lock` 与 `sdkconfig.defaults` 属于可重建输入；`sdkconfig`、`build/` 和本机 IDE 配置不是发布源码。

项目使用 `MINIMAL_BUILD`。`managed_components/` 是锁定依赖的本地副本，可用于当前环境稳定构建；如果删除，ESP-IDF Component Manager 应按锁文件恢复。不要修改第三方组件来绕过应用层问题。

### 3.2 管理中心

开发环境需要 Python 3.11+。测试不要求启动外部服务：

```powershell
python -m unittest discover -s bridge\tests -v
python -m compileall -q bridge
```

从源码启动：

```powershell
python bridge\bridge_app.py --open-dashboard
```

登录自启动写入 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\DotiiManagementCenter`。源码模式指向绝对路径的 `pythonw.exe -B bridge_app.py --startup`；冻结模式必须通过 `sibling_executable()` 指向同目录 `DotiiManagementCenter.exe --startup`，不能使用正在处理 API 的 `DotiiBridge.exe` 的 `sys.executable`。只要用户仍保持开关启用，后台每次正常启动都会把旧命令修复为当前运行入口。

运行数据位于 `%LOCALAPPDATA%\StateDisplay`。测试必须使用临时目录，不能复用真实令牌、Bambu 访问码或用户资源。

## 4. 配置与路径规则

源码和发布程序都必须通过 `runtime_paths.py` 定位资源，禁止新增本机绝对路径。

- `application_root()`：源码根目录或 EXE 所在目录。
- `bundle_root()`：源码资源目录或 PyInstaller 解包目录。
- `resource_path()`：网页、图标、种子状态和固件资源。
- `tools_root()`：可选的应用私有工具目录。
- `%LOCALAPPDATA%\StateDisplay`：可写运行目录。

不得在源码中固定用户名、盘符、COM 口、局域网 IP、Wi-Fi、访问码或令牌。环境变量只作为兼容回退，缺失时必须安全失败。

## 5. schema_version 兼容规范

当前设备协议为 `schema_version = 1`。ESP32 通过 `/api/v1/snapshot` 获取状态；自定义 RGB565 帧使用独立资源接口。管理接口只允许回环地址访问。

兼容规则：

1. 新字段优先做成可选字段，不改变旧字段含义。
2. 固件对缺失、类型错误、超长和越界值提供安全默认值。
3. 管理中心写入前先校验和裁剪，不能把秘密写入设备快照。
4. 不兼容变更必须提升 `schema_version`，并保留至少一个迁移或明确拒绝路径。
5. 缺失的 Codex 额度、Token 或时间显示 `--`，不能从日志或其他数据推算。
6. 所有字符串、数组、图片和 HTTP body 均须有长度上限。

核心可选模块字段：

```json
{
  "schema_version": 1,
  "modules": {"codex": false, "bambu": false, "custom": true, "dotii": true},
  "dotii": {
    "expression": "idle_breath",
    "state": "idle",
    "state_assigned": false,
    "state_duration_ms": 0,
    "state_hold": false,
    "state_token": 0
  }
}
```

旧管理中心或旧固件缺少 Dotii 字段时，页面必须回到空闲状态；不得导致页面循环、触摸或电源逻辑失效。

## 6. 管理中心 API

管理中心默认监听 `8787` 端口。修改或扩展接口前先查看 `BridgeHandler` 的现有路由和对应前端调用。

- `GET /health`：服务存活检查。
- `GET /api/v1/snapshot`：ESP32 状态快照，需要设备令牌。
- `GET /api/v1/custom/frame`：自定义 RGB565 帧，需要设备令牌。
- `POST /api/v1/admin/codex/check`：Codex 手动只读运行检测，仅在模块启用后开放。
- `/api/v1/admin/*`：本机管理接口，只接受回环地址。
- `/assets/*`、网页 JS/CSS：本机管理界面静态资源。

新增管理接口时必须：限制为回环地址、校验 Content-Type 和 body 大小、返回稳定 JSON 错误、避免输出秘密、为成功和失败路径添加测试。设备接口还必须验证令牌。

## 7. 新模块开发流程

一个模块只有完成以下链路才算实现：

1. 在管理中心增加配置模型、持久化和默认值；首次运行不得产生外部副作用。
2. 增加本机管理 API 和同一套网页配置，不为开发版与发布版维护两套逻辑。
3. 在设备快照加入可选字段并更新校验。
4. 在固件 `app_state` 中解析，提供缺失字段回退。
5. 在 `state_ui` 注册页面、刷新策略和圆屏安全区。
6. 接入页面启用/关闭、页面循环、熄屏恢复和设置页行为。
7. 添加 Python 测试、静态检查和 ESP-IDF 构建验证。
8. 最后再进行真实服务、网络和设备验收。

Codex 和 Bambu 的默认值必须保持关闭，Dotii 与自定义页面默认开启。用户启用功能之前，不检查、安装、下载、登录或连接外部服务。

## 8. Codex 数据边界

固定版 Codex CLI 通过 `codex app-server --listen stdio://` 提供数据。管理中心只处理官方公开结构化响应，包括账户限额、用量桶、任务列表和任务详情。

- 不读取 `auth.json`、内部会话文件、rollout JSONL、隐藏推理或工具参数。
- 单个历史任务因未知 item 类型无法解析时，应隔离该任务并继续读取其他任务。
- 全量暂时不可读时可保留上一次有效快照，但必须显示真实运行状态。
- 同时运行多个任务时，排序和选中逻辑必须稳定。
- 原始 Time/Token 字段缺失时保持不可用，不进行估算。
- Codex 设置中的运行检测只在用户点击后启动短生命周期 App Server，依次验证 `account/read`、`account/rateLimits/read`、`account/usage/read` 和 `thread/list`。响应只包含展示所需的账户信息与分项状态，不返回令牌、命令路径、任务内容或内部日志。

发布包固定包含 Node.js 24.16.0、npm 11.13.0 和 `@openai/codex` 0.151.0。升级任一组件后需重新验证 App Server 协议、登录复用、许可和包体校验。

## 9. Bambu 数据与控制

Bambu 使用打印机局域网 MQTT/TLS，不使用未经支持的云端逆向接口。配置包含 IP、序列号、访问码和相机开关；访问码只保存在本机。

状态至少覆盖空闲、准备、打印、暂停、完成和失败。控制按钮必须由当前状态约束：只能在允许时暂停、继续或停止，不能因页面刷新或普通触摸触发。

打印状态不依赖 FFmpeg。仅当模块启用、配置完整、相机开启且 MQTT 提供合法 RTSPS 地址时解析 FFmpeg。旧式 6000 端口 JPEG 链路不使用 FFmpeg。内置版本为 BtbN Windows x64 LGPL-only FFmpeg `n8.1.2-50-g1a748fe2cd-20260831`；更换构建时禁止误用 GPL-only 组件，并重新审核许可证。

## 10. 蓝牙配网与固件烧录

蓝牙负责扫描 Dotii、读取状态和下发 Wi-Fi、管理中心 URL 与设备令牌。配置体采用分包写入；首次未绑定设备依赖 Windows 加密 GATT 链路，后续配置还要校验当前设备令牌。

固件烧录流程：

1. 从共用的 `firmware/flasher_args.json` 读取镜像与偏移；开发构建完成后由打包脚本从 `build/` 原子更新该目录。
2. 将所有路径限制在构建资源目录内。
3. 计算镜像 SHA-256，并读取应用版本。
4. 只列出 VID `303A`、PID `1001` 的 Dotii ESP32-S3 USB 串口。
5. 用户明确确认后调用内置 esptool。

VBS 源码入口不要求从已激活的 ESP-IDF 终端启动。烧录器会依次检查当前 Python、`IDF_PYTHON_ENV_PATH`、`IDF_TOOLS_PATH`、用户目录下的官方 ESP-IDF Python 环境和系统盘 Espressif 官方安装目录；候选环境必须同时包含 esptool 与 `targets/stub_flasher/*/esp32s3.json`，否则不得把烧录功能标记为就绪。
6. 依据退出码和设备端哈希校验判断成功。

常规烧录不擦除 NVS。不得开放任意串口、任意镜像路径或默认整片擦除。自动化测试不能连接或写入真实设备。

## 11. 自定义页面与图像

配置和处理后的资源保存在 `%LOCALAPPDATA%\StateDisplay`。上传原图限制为受支持的 PNG/JPEG 与 4 MiB 以内；浏览器完成裁切和缩放后再提交结果。最终帧为 466 × 466、RGB565、大端字节序，大小固定为 434312 字节。

固件按 revision 更新缓存。下载失败保留上一次有效帧；没有有效帧时显示安全默认页，不应反复重启或阻塞其他模块。

## 12. 圆屏交互和性能

- 重要文字和卡片保持在圆屏安全区，背景使用 AMOLED 真黑。
- 右滑是子页面唯一返回手势，不增加可见返回按钮。
- Button A 保留页面切换和既有控制界面语义。
- 危险操作必须明确确认，不能绑定普通滑动或触摸。
- 显示基线为 `MADCTL=0x00`、原生 gap `(6,0)` 和 BSP 软件合成旋转。
- 动态页面使用局部刷新；最终 RGB565 脏矩形在物理发送层偶数对齐。
- 当前旋转使用 12 行 DMA 缓冲，Wi-Fi 静态 RX 缓冲为 10。
- 新增中文文本前评估字形集合、Flash、PSRAM 和帧耗时。

## 13. 代码规范与清理

- Python 使用类型提示、`pathlib.Path`、有界输入和明确异常；后台线程必须可停止。
- C 模块按职责拆分，跨模块状态通过头文件公开的最小接口访问。
- JavaScript 只引用已存在 DOM id；页面关闭后相关内容完全折叠。
- 不保留 `__pycache__`、`.pyc`、IDE 绝对路径、日志、临时脚本或旧发布包。
- 所有临时、中间和预览文件统一放在项目根目录 `.codx/`，任务完成后清理不再需要的内容。
- 生成资源的注释不得包含用户名或本机绝对路径。
- 项目自有说明只保留根目录的 `README.md` 与 `开发指南.md`；发布目录和 ZIP 不复制这两份文档。
- 删除代码前先通过引用搜索和测试确认；第三方依赖内的示例代码不作为 Dotii 业务冗余随意修改。

## 14. 测试清单

提交前至少执行：

```powershell
python -m unittest discover -s bridge\tests -v
python -m compileall -q bridge
node --check bridge\web\app.js
idf.py build
```

发布前补充：

- 管理网页主要页面和模块开关交互检查；浏览器控制台无错误。
- 从隔离用户目录启动两个 EXE，验证 `/health`、资源加载和退出清理。
- 校验发布目录 `SHA256SUMS.txt`，解压 ZIP 后再次启动。
- 秘密扫描、绝对路径扫描、Markdown 文件数量和第三方许可检查。
- 新 Windows 用户目录、跨电脑和跨网络验证。
- 新设备烧录、Wi-Fi/蓝牙、圆屏显示、触摸、按键、电源和休眠真机验收。
- Codex 登录与任务更新、Bambu MQTT/相机/控制真实集成测试。

源码构建、EXE 启动、真实服务和实体设备是不同验收层级，记录时必须分别说明。

## 15. Windows 发布流程

1. 确认 `CMakeLists.txt` 版本与发布目标一致。
2. 运行全部管理中心测试和静态检查。
3. 使用 ESP-IDF 6.0.2 重新构建固件，确认 `build/flasher_args.json` 与镜像完整；打包脚本会更新 VBS/EXE 共用的 `firmware/`。
4. 执行 `packaging\build_windows.ps1 -Clean`。
5. 检查两个 EXE 的产品版本、启动参数和 `/health`。
6. 校验发布目录逐文件 SHA-256 和 ZIP SHA-256。
7. 安装 Inno Setup 时，以 `/DAppVersion=1.0.0` 编译 `.iss` 生成安装器。
8. 完成干净环境与真机测试后再对外发布。

打包脚本从固件版本自动派生发布版本，并拒绝不一致的 `-Version` 参数。发布目录包含两个 EXE、固定工具链和校验清单，不重复包含根目录说明文档，也不依赖本机 Python、Node.js 或 FFmpeg。

`DotiiBridge.spec` 必须使用 `collect_data_files("esptool")` 收集 flasher stub JSON。后台桥接使用控制台型 PyInstaller bootloader，但始终由托盘程序以 `CREATE_NO_WINDOW` 隐藏启动，以便 esptool 子进程可以把进度和错误回传给管理中心而不弹出未处理异常窗口。两个 EXE 使用独立、ASCII 安全的 Windows 版本资源文件；中文字段写成 Python Unicode 转义，避免被构建环境代码页错误解码。

## 16. 第三方组件与许可证

- ESP-IDF 管理组件版本记录在 `dependencies.lock`，各组件许可证文件保留在组件目录。
- 本地 Waveshare BSP 基于 3.0.1，目录内保留原始许可证。
- Node.js 24.16.0 与 npm 11.13.0 的许可证文本位于 `tools/node`。
- OpenAI Codex CLI 0.151.0 使用 Apache-2.0，完整文本和包元数据位于 `tools/codex-cli`。
- FFmpeg 使用 BtbN Windows x64 LGPL-only 构建，许可证和构建说明位于 `tools/ffmpeg`。
- Windows EXE 还包含 PyInstaller 6.22.2、Bleak 3.0.2、esptool 5.3.1、Pillow 11.3.0 及其依赖。

升级或裁剪依赖时不得删除法律要求保留的许可证文本、版权声明和包元数据。Markdown 格式的第三方许可证可等内容改名为 `.txt`，但不得仅为目录整洁而丢失许可内容。

## 17. 已知风险

- Bambu 字段与相机链路可能随打印机固件变化。
- 电脑局域网地址变化后可能需要重新蓝牙同步。
- 一键烧录仍依赖可识别的 Dotii USB 设备和有效固件包。
- PyInstaller 单文件程序启动时会解包到临时目录，安全软件可能增加首次启动时间。
- 固件构建和桌面测试不能替代实体圆屏、触摸、电源和射频验收。
